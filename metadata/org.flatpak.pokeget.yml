#!/usr/bin/env bash

echo "
app-id: com.github.pokeget
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
command: pokeget
modules:
  - name: pokeget
    buildsystem: simple
    build-commands:
      - install -D pokeget /app/bin/pokeget
    sources:
      - type: file
        path: pokeget
finish-args:
  - --filesystem=home
  - --share=network
"